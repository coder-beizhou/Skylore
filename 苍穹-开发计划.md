# 苍穹 · Firmament Reader —— PC 小说阅读器开发计划

> 一款仿番茄小说 / 微信读书 PC 版体验的本地桌面小说阅读器
> 中文名：**苍穹**　英文名：**Firmament Reader**（备选：Aether Reader / Azure Reader）

---

## 一、产品定位

对标番茄小说 PC 版 + 微信读书桌面版的**沉浸式阅读体验**，但做成本地纯粹、无广告、无登录、格式全能的阅读器。

**一句话定位：** 一个能安静读完一本书的桌面软件。

**核心体验目标（三条不可妥协的线）：**

| 目标 | 含义 | 验收方式 |
|---|---|---|
| **排版可控** | 字体/字号/行距/字距/边距全参数实时生效 | 拖动滑块，正文立即重排，无需重进 |
| **沉浸无扰** | 阅读时控件自动淡出，全屏无边框 | 全屏阅读时鼠标静止 3 秒，所有 UI 归隐 |
| **秒级隐藏** | 摸鱼模式一键切换，无感知延迟 | 触发到伪装界面呈现 < 150ms |

---

## 二、技术选型（已实测验证）

| 层 | 选型 | 决策理由 |
|---|---|---|
| 运行时 | **Electron 44.4.3** | 满足"能读本地目录 / 系统字体 / 无边框沉浸 / 本地持久化"四项硬需求 |
| 主进程 | Node.js（CommonJS） | 文件 IO、字体枚举、全局热键、窗口控制 |
| 渲染层 | **原生 ES Module + HTML/CSS（无框架、无打包器）** | 避免构建链风险；本项目交互逻辑可控，框架只会增加调试成本 |
| EPUB 解析 | **JSZip + 自研 OPF/Spine 解析** | ⚠️ **不用 epub.js**——它用 iframe 渲染，主题/字体无法统一控制。自研可让 TXT 与 EPUB 走**同一套渲染管线**，这是"换背景换字体"能完美工作前提 |
| TXT 解析 | **jschardet（编码嗅探）+ iconv-lite（GBK/GB18030 解码）** | 中文小说 TXT 大量是 GBK，不做编码嗅探会全是乱码 |
| 持久化 | 主进程 JSON 文件（userData 目录）+ 本地封面缓存 | 无外部依赖，数据可迁移、可备份 |
| 打包 | **electron-builder**（NSIS 安装版 + Portable 免安装版） | 一键出双形态产物 |

### ⚠️ 环境实测结论（关键风险已排除）

实测本机网络环境，发现一个**必须提前规避的坑**：

```
registry.npmjs.org        → 200  OK
cdn.jsdelivr.net          → 200  OK
registry.npmmirror.com    → 200  OK
github.com                → 000  ✗ 不可达
npmmirror /binary/electron → 302 OK （二进制镜像可用）
```

**问题：** Electron 安装时默认从 `github.com/electron/electron/releases` 下载 ~100MB 二进制包，而 GitHub 在本机**不可达**，直接 `npm install electron` 会卡死或失败。

**对策（已写入实施方案）：** 安装前设定镜像环境变量

```bash
ELECTRON_MIRROR=https://registry.npmmirror.com/-/binary/electron/
ELECTRON_BUILDER_BINARIES_MIRROR=https://registry.npmmirror.com/-/binary/electron-builder-binaries/
```

> 结论：**可行**。npmmirror 二进制镜像返回 302（正常，指向其自有 CDN），可完成下载。

### 附带环境说明

本机 bash 输出捕获异常（`dir` 未设导致外部命令 PATH 丢失），**已确认规避方案**：命令结果重定向到文件后读取。开发全程用 PowerShell / 文件重定向方式执行命令，不影响交付。

---

## 三、目录结构

```
苍穹-Firmament/
├─ package.json
├─ electron-builder.yml
├─ src/
│  ├─ main/                      # 主进程
│  │  ├─ main.js                 # 应用生命周期 / 窗口管理 / IPC 注册
│  │  ├─ preload.js              # contextBridge 安全桥接（contextIsolation 开启）
│  │  ├─ store.js                # JSON 持久化（书架 / 设置 / 进度）
│  │  ├─ ipc/
│  │  │  ├─ books.js             # 导入 / 扫描 / 章节读取
│  │  │  ├─ settings.js          # 排版与主题读写
│  │  │  └─ boss.js              # 摸鱼模式全局热键与窗口伪装
│  │  └─ services/
│  │     ├─ encoding.js          # 编码嗅探与解码
│  │     ├─ txt-parser.js        # TXT 章节切分
│  │     ├─ epub-parser.js       # EPUB → 结构化章节
│  │     └─ fonts.js             # 系统字体枚举
│  └─ renderer/                  # 渲染进程
│     ├─ index.html
│     ├─ styles/
│     │  ├─ tokens.css           # 设计变量（颜色/圆角/阴影/动效曲线）
│     │  ├─ base.css
│     │  ├─ skins/               # 皮肤：minimal.css（极简）/ skeuomorphic.css（拟物）
│     │  ├─ themes/              # 主题：day / night / eye-care
│     │  └─ {bookshelf,reader,settings,boss}.css
│     ├─ js/
│     │  ├─ app.js / router.js
│     │  ├─ views/{bookshelf,reader,settings}.js
│     │  ├─ engine/
│     │  │  ├─ paginator.js      # 分页引擎（CSS 多列 + 测量）
│     │  │  ├─ scroller.js       # 滚动引擎（连续流）
│     │  │  └─ autoturn.js       # 自动翻页 / 自动滚动调度器
│     │  └─ lib/{fonts,theme,toast}.js
│     └─ assets/
│        ├─ textures/            # 纸纹 / 木纹 / 皮革 / 星空（SVG + CSS 生成，不塞大图）
│        ├─ fonts/
│        └─ icons/
└─ build/                        # 应用图标
```

---

## 四、核心模块设计

### 4.1 解析引擎（格式层）

**统一输出契约** —— 无论 TXT 还是 EPUB，最终都归一成同一结构：

```js
{
  meta: { title, author, cover, format, encoding },
  chapters: [ { index, title, html } ]   // html 已是净化后的正文片段
}
```

- **TXT 解析**：嗅探编码 → 全文解码 → 正则切章
  章头识别覆盖：`第X章/节/回/卷`、`Chapter N`、`序章/楔子/尾声/后记`、`０-９全角数字`、纯数字行
  兜底策略：**无章头时按固定字数自动分段**（约 3000 字/段），保证任何 TXT 都能读
- **EPUB 解析**：`container.xml` → OPF（元数据 + 封面 + manifest）→ `spine` 顺序 → 逐章 XHTML
  正文净化：剥离 `<script>/<style>`、内联样式、`position` 等破坏排版的属性，**只保留语义标签**，让全局字体与主题接管

> **为什么这点很关键：** 如果保留 EPUB 自带样式，用户换字体就会出现"有的章节变了、有的没变"。净化是排版统一的前提。

### 4.2 分页引擎（翻页模式）

采用 **CSS 多列 + 测量重排** 方案：

```
容器设 column-width = 可用宽度, column-gap = 栏间距
整章内容溢出成 N 栏 → 每栏即一页
翻页 = 容器 translateX(±(页宽+栏间距))
```

- 重排触发点：字号 / 行距 / 字距 / 字体 / 页边距 / 窗口尺寸 / 字体加载完成（`document.fonts.ready`）
- 字体加载完成必须重排——否则行高变化会导致分页错位，这是同类软件最常见的 bug
- 翻页动画：可切换 `平滑位移` / `仿真翻页（3D 折角）` / `无动画`

### 4.3 滚动引擎（上下滑动模式）

- 连续长文流，`scrollTop` 平滑推进
- **自动滚动**：`requestAnimationFrame` 累加亚像素位移，速度可调（x px/秒）
  - 关键细节：用累计浮点值 + `scrollTo`，而非 `scrollTop++`（整数会卡顿、丢精度）
  - 交互暂停：鼠标移入正文 / 手动滚动 → 暂停；移出 3 秒 → 恢复

### 4.4 字体系统

**三层字体来源：**

1. **系统字体自动探测** —— 运行时用 canvas 宽度比对法检测本机真实可用字体，只展示装了的
   - 宋体 `SimSun`、黑体 `SimHei`、楷体 `KaiTi`、微软雅黑 `Microsoft YaHei`
   - 圆体 `YouYuan 幼圆`、等线 `DengXian`
   - 手写体 `STXingkai 华文行楷`、`STXinwei 华文新魏`、`FZShuTi 方正舒体`
2. **内置开源手写体** —— 附赠 1 款 SIL OFL 授权字体，保证任何机器都有真手写体可选
3. **用户导入字体** —— 支持拖入 `.ttf/.otf/.woff2`，注册为自定义字体，全局可用

> **为什么用字体探测而不是硬编码列表：** 硬编码会在没装该字体的机器上静默回退到默认字体，用户以为"换字体没用"。探测能保证**列表里每一个都真的能变**。

### 4.5 主题与背景系统

- **三套基色主题**：日间（暖白）／ 夜间（深灰，非纯黑，降低眩光）／ 护眼（豆沙绿 `#C7EDCC` 系）
- **背景库**：纯色 6 种 + 纹理（纸纹 / 木纹 / 皮革 / 羊皮卷 / 星空 / 云纹），纹理用 **SVG/CSS 生成**而非大图，控制体积
- **对比度自适应**：选定背景后自动推导正文色与控件色，保证任何背景都能读清（背景非纯色时叠加半透明衬底）
- **两套皮肤**（已确认需求）：
  - `极简` —— 微信读书风，通透留白，控件毛玻璃淡出
  - `拟物` —— 纸质书卷风，书页阴影/弧度、木质书架、皮革边框
  - 一键切换，阅读进度与设置全共享

### 4.6 摸鱼模式（新增需求）

| 能力 | 实现 |
|---|---|
| **全局老板键** | `globalShortcut` 注册，窗口失焦也生效，默认 `Alt+`` （可自定义） |
| **瞬间伪装** | 预加载 4 套伪装界面，切换仅改 DOM 可见性不走网络，< 150ms 呈现 |
| 伪装界面 | ① 仿 Word 文档（带滚动长文）② 仿 Excel 数据表（带行列号）③ 仿 VS Code 代码编辑器（带语法高亮）④ 仿邮件客户端 |
| 快速隐藏 | 窗口透明度降至 8% / 缩为屏幕角落迷你条 / 直接最小化 |
| 静音 | 切换时自动静音，避免声音暴露 |
| **恢复** | 同一热键切回；伪装界面内双击标题栏 / `Ctrl+Shift+Alt+R` 兜底恢复 |
| 记忆 | 恢复后精确回到原章节与原页码 |

### 4.7 数据模型

```
userData/
├─ books.json        # 书架：id, 书名, 作者, 格式, 路径, 封面, 分组, 添加时间
├─ progress.json     # 阅读进度：书 id → 章节索引, 页/偏移, 时间戳, 阅读时长
├─ settings.json     # 全局排版默认值 + 主题 + 热键 + 皮肤
├─ bookmarks.json    # 书签与笔记（二期）
└─ covers/           # 封面缓存（无封面时按书名字体自动生成渐变封面）
```

---

## 五、分阶段计划

| 阶段 | 内容 | 交付与验收 |
|---|---|---|
| **P0 工程搭建** | 项目初始化、配置镜像源、装 Electron、目录骨架、无边框窗口、IPC 骨架 | ✅ 双击启动出主窗口，前后端能互通 |
| **P1 解析引擎** | 编码嗅探、TXT 切章、EPUB 解析、书架持久化、导入（选择/拖拽/文件夹扫描） | ✅ 导入 GBK 的 TXT 与 EPUB，均能得到正确章节数组无乱码 |
| **P2 阅读引擎** ⭐ | 分页引擎、滚动引擎、自动翻页、自动滚动、章节跳转、进度续读、键鼠交互 | ✅ 两种模式流畅切换；连续调整字号 20 次无错位、无残留 |
| **P3 排版与主题** ⭐ | 字体系统、字号/行距/字距/段距/边距、三主题、背景库、双皮肤 | ✅ 所有参数实时生效并持久化；重启后完全还原 |
| **P4 书架与主界面** | 书架网格、自动生成封面、进度环、分组/排序/搜索、最近阅读、全局设置页 | ✅ UI 完整美观，空态/加载态/异常态均有设计 |
| **P5 摸鱼模式** | 全局老板键、4 套伪装界面、透明度/迷你条、恢复机制、记忆位置 | ✅ 触发 < 150ms；任意窗口焦点下热键可用 |
| **P6 打包交付** | electron-builder 配置、图标、NSIS 安装版 + Portable 免安装版 | ✅ 产出可双击运行的 exe，干净机器可启动 |

> ⭐ 标记的 P2 / P3 是**体验成败的核心**，也是同类软件最容易做糙的地方，会重点打磨。

---

## 六、风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| GitHub 不可达导致 Electron 装不上 | 阻塞 P0 | 已实测，用 npmmirror 二进制镜像（方案已验证） |
| EPUB 内联样式污染全局排版 | 换字体部分失效 | P1 强制净化，只保留语义标签 |
| 字体加载异步导致分页错位 | 翻页串行/丢字 | `document.fonts.ready` 后强制重排 |
| 大文件（10MB+ TXT）卡顿 | 打开迟钝 | 章节懒解析：只解析当前章 ±1，索引常驻内存 |
| 硬件/系统字体差异 | 手写体缺失 | 内置开源手写体兜底 + 支持用户导入 |
| 拟物皮肤易显廉价 | 观感不达标 | 用 SVG/CSS 精绘而非贴图；先在 P3 出一版给你看效果再深化 |

---

## 七、待你确认的三点

1. **英文名**：暂定 `Firmament Reader`，备选 `Aether Reader` / `Azure Reader`，你定一个。
2. **P2 验收顺序**：我倾向先把**阅读引擎 + 排版主题**（P2+P3）做到位给你验收，再补书架视觉（P4）——阅读手感是灵魂。你是否认可这个优先级？
3. **手写体**：内置开源手写体（约 +5MB 体积）还是只用系统字体 + 自行导入（0 体积）？

---

*计划版本 v1.0　|　生成时间：2026-09-19*

---

## 八、v1.1.0 迭代记录（摸鱼模式强化 / 透明浮窗 / 快捷键）

本次迭代围绕「伪装做得像、藏得住、用得顺」三件事，全部改动均已通过冒烟测试（144 项断言）。

### 1. 四个伪装界面：从贴图变成「真能读」

改动前四个伪装界面是纯静态贴图，里面没有小说、不能滚动，窗口也不能拖动。

| 界面 | 新增的真实感元素 | 小说位置 |
|---|---|---|
| Word | Win11 标题栏（快速访问工具栏 / 文件名 / 账号 / 窗口按钮）、Ribbon 选项卡 + 分组（剪贴板/字体/段落/样式）、标尺、状态栏 | 文档正文（抬头之后） |
| Excel | Win11 标题栏、Ribbon 分组、名称框 + 编辑栏 + Σ、Sheet1/2/3 标签栏、状态栏缩放条 | 右侧 G/H/I 列「备注说明」 |
| VS Code | 标题栏、面包屑、minimap（按正文行数生成）、底部面板（问题/输出/终端）、蓝色状态栏 | 打开的 `notes.md` |
| Outlook | 标题栏、文件夹树（含未读计数与分组）、未读加粗邮件列表（发件人/主题/预览/时间）、邮件头（头像/发件人/操作按钮） | 阅读窗格（邮件头下方） |

**统一采用「固定抬头 + 可变正文」两段式结构：**

- 抬头永远不变（会议信息、备注表头、文件注释、邮件头），保证看起来是一份完整文件
- 正文是当前章节小说，随阅读进度更新
- **首次进入某个伪装界面时停在顶部**，先让人看到完整外壳；之后滚动才跟随阅读位置
- 定位时扣掉抬头高度，保证「滚到哪 → 存哪」可逆（否则每次进出都会往前漂）

### 2. 伪装界面全部支持翻页与跨章拼接

| 操作 | 行为 |
|---|---|
| 点击左 / 右两侧（各 26% 宽） | 翻一屏（视口高度的 86%） |
| 滚轮 | 按行滚动（原生手感） |
| `PageUp` / `PageDown` / `Space` | 翻一屏 |
| `↑` / `↓` | 小步滚动 90px |
| 滚到章末 / 章首 | **自动接续下一章 / 上一章**（跨章连续阅读） |

### 3. 新增「透明迷你框」形态

用**独立透明窗口**（`transparent: true`）承载正文，主窗口自动隐藏 ——
于是文字直接浮在桌面 / 其他软件之上，背景完全通透。

关键实现决策与踩坑：

1. **为什么不把主窗口整体变透明** —— 主窗口承载书架、设置、分页与连续滚动引擎，改它的透明属性会连带影响窗口阴影、缩放、最大化行为；独立浮窗对现有功能零影响。
2. **`toggle()` 里绝不能 await `executeJavaScript`** —— 进入时主窗口刚被 `hide()`，反向询问阅读位置可能迟迟不返回，一旦 await 整个「进入摸鱼」就卡死（实测挂住两分钟）。改成「拿到就推」+ 2.5 秒超时。
3. **Electron 没有 `isTransparent()` / `isSkipTaskbar()`** —— 诊断代码里调用它们会直接抛 `TypeError` 把整个接口打挂，改为创建时记录属性 + 诊断函数逐项 `try`。
4. **`webContents.send` 不能序列化 Promise** —— 把 `getReaderSnapshot()` 的返回值直接塞进去会抛 `Failed to serialize arguments`，浮窗永远拿不到正文。

### 4. 迷你框双角拖拽缩放（普通 + 透明）

左下角与右下角**都能**拖拽调整大小：

- 右下角：左边缘为锚点（向右下生长）
- 左下角：右边缘为锚点（贴屏幕右侧时向左下生长 —— 这才是自然方向）
- 两种锚点都做工作区边界校验，窗口不会被拖出屏幕够不着
- 拖动时显示实时尺寸（`420 × 300`），松手记忆到设置
- 设置页保留「正方形边长」滑杆 + 新增「恢复正方形」按钮

配套修正：迷你框容量计算改为**从真实 DOM 尺寸读取**。
原先按 `settings.miniBoxSize`（正方形）推算，拖成长方形后拉宽会算少行数（填不满）、拉高会算多行数（末行被裁）。

### 5. 新增快捷键设置面板

全应用快捷键收敛到一张表（`src/renderer/js/lib/keymap.js`），设置页可逐项录制修改：

- 12 个可自定义动作（阅读页 8 个 + 全局 4 个）
- **录制时实时冲突检测**：撞键直接拒绝并指出被谁占用
- 每项可单独恢复默认，也可一键全部恢复
- 改动立即生效（每次按键重新查表），无需重启

**刻意不开放** `Esc` 与老板键 —— 它们承担「任何情况下都能退出」的兜底职责。

### 6. 阅读页 UI 显隐行为变更

用户明确要求：**滚动时不要弹出工具栏，只有点击才显示**。

- 鼠标移动不再唤起顶栏/底栏（原先稍动鼠标就闪出来，很干扰）
- 滚动阅读时若工具栏正显示，会主动收起
- 点击正文中部切换工具栏显隐（保留）
- 鼠标已在工具栏区域时延长停留时间（正要去点按钮，别中途消失）

### 7. 测试与验证

- 冒烟测试从 118 项扩到 **144 项**，新增：伪装界面装载小说、可滚动、点击翻页、键盘翻页、章末跨章接续、双角缩放锚点、容量随尺寸变化、透明浮窗真透明 + 主窗口隐藏 + 退出后销毁
- 纯 Node 测试 74 项全通过（解析器 / Library / 编码 / EPUB 净化）
- 截图验证四个伪装界面 + 透明浮窗（含像素级 alpha 采样，确认背景确实透明）
- 冒烟测试顺带修掉三条**空断言**（`ok(..., true)` 这类永远通过、什么都不验证的写法）

### 本次踩过的坑（都写进了代码注释）

| 坑 | 表现 | 根因 |
|---|---|---|
| 静态常量未声明 | 迷你框被钳到 120×120 | `Math.max(undefined, 300)` = NaN，静态字段那次补丁没落上 |
| `.fake-page` 有 `overflow: hidden` | Word 里整章只显示一屏，翻页无反应 | 纸张高度被压成一屏，内容根本没变长 |
| 复用旧容器 ID | 邮件列表和正文全空白 | `buildMailBox` 里校验 `#fakeMailView`（已改名 `#fakeMailScroll`），整段提前 return |
| 程序化设置 `scrollTop` 触发事件 | 「进来先看完整外壳」被自己抹掉 | scroll 回调把程序设的位置当用户滚动回写了 |
| `white-space: pre-wrap` 渲染注释头 | VS Code 抬头高 615px（比视口还高） | 多行注释按最长行撑开，正文被挤出屏幕 |

---

## 九、v1.1.1 迭代记录（透明框去泛光 / 窗口瘦高 / 测试后台化）

### 1. 透明迷你框：去掉白色泛光

透明框的正文原本带一圈白色 text-shadow（当初想的是"任何壁纸都看得清"），
结果在深色背景上整片字周围糊出白雾，比看不清更难看。

现在**纯黑字、零描边零泛光**（`--ink: #000000`），放在浅色底上看；
深色壁纸可在设置里切"浅色字"模式。

> 验证方式：截图后统计像素 —— 白色像素 **0.00%**，非透明像素从 48.1% 降到 17.1%。

### 2. 窗口改成瘦高 + 可收窄到 380px

| 项 | 旧 | 新 |
|---|---|---|
| 默认尺寸 | 1180×800（矮胖） | **900×960（瘦高）** |
| 最小宽度 | 520px | **380px** |

配套修复：

1. **`WINDOW_GEOMETRY_VERSION` 迁移机制** —— 否则 `window.json` 里的旧尺寸会
   一直覆盖新默认值，改了等于没改。递增版本号即让旧数据重置一次。
2. **窗口位置越界纠正** —— 实测用户数据里存着 `x=2691`（屏幕才 2560 宽），
   窗口大部分在屏幕外。现在启动时校验位置，越界就回落默认位置。
3. **`win:set-size` 的硬编码下限 420 去掉** —— 它比窗口实际允许的最小值还大，
   导致"放开了最小宽度还是窄不下去"。

### 3. 极窄窗自动收边距（两个 CSS/JS 深坑）

380px 窗口里，左右各 88px 的边距会吃掉 176px，正文只剩 200px（约 10 字/行）。
现在边距自动收敛到窗口宽度的 6%（实测 24px，每行 18.2 字）。

踩到的两个坑：

| 坑 | 现象 | 根因 |
|---|---|---|
| CSS 自定义属性**自引用** | 边距变成 **0px**（正文贴边） | `--reader-ml: min(var(--reader-ml), 6vw)` 是自引用；改名成 `--ml-user: var(--reader-ml)` 再引用回去仍构成循环引用，同样算成 0px |
| `parseFloat` 读函数表达式 | 边距"收敛"完全失效，仍是 88px | `parseFloat('min(88px, 6vw)')` = `NaN`，函数静默回落到默认值 88 |

最终方案：**JS 写原始值（`--reader-m-l-raw`）+ CSS 读它收敛**（单向依赖，不循环）；
`Paginator.readMargins()` 遇到函数表达式时，**在正文元素本身**上临时写一次 padding 实测像素值
（挂到 `body` 上量不准 —— `vw` 是相对视口的，容器宽度可能被侧栏挤压过）。

### 4. 测试改为后台运行

冒烟 / 截图 / 探针一律**不显示在用户屏幕上**：窗口被移到所有显示器工作区之外再 `showInactive()`
（见 `parkOffscreen()`）。

> ⚠ **不要改回 `hide()`**。隐藏窗口不参与真实布局，实测 DOM 坐标算到屏幕外几千像素
> （章末导航按钮测出 x=5822），所有坐标相关断言集体误报。
> 「移到屏幕外」既让用户看不见，又保证渲染与测量真实可信。

### 5. 构建流程：不再需要备用目录

- `npm run build` 现在会先跑 `build/close-running.js`：自动优雅关闭"正从本项目 `dist/` 运行"的实例
  （Windows 不允许覆盖运行中的 exe/dll，否则报 Access is denied）。
  只认本项目 `dist/` 下的进程，别人另存到别处的苍穹不会被误杀。
- `build/clean.js` 已接入 build 链，每次打包前清掉旧 `win-unpacked` / `run` / 旧版本 zip，
  `dist/` 里始终只有当前版本。
- 详见 `AGENTS.md`（开发约定）。

---

## 十、v1.1.2 迭代记录（伪装翻页 / 透明框字色 / 窗口按钮右置）

### 1. 伪装界面翻页修复

根因：boss.js 与 reader.js 都在 document 上监听 keydown 且都 preventDefault，
伪装激活时阅读器 handler 仍执行 nextPage()/prevPage()，与伪装容器滚动互相覆盖。

修复：boss 侧 stopPropagation + reader 侧 isActive 守卫（双保险）。
新增整窗点击翻页（.fake 根节点，左右各 26%）与右下角可见翻页按钮。

### 2. 透明框字色跟随主题

根因：main 进程 snapshot 的 dark 字段误用 overlayInk 字符串，主题切换不生效。
修复：snapshot 改送 themeDark（主界面真实主题）+ ink（用户档位 auto/dark/light）。

### 3. 主窗口按钮右置

根因：窄屏媒体查询隐藏 brand/center 后，.titlebar__right 成为唯一 flex 子项且无 margin-left:auto。
修复：base.css 给 .titlebar__right 加 margin-left:auto。

### 4. 伪装滚动残行

根因：滚动容器的 padding 属于 scrollport，滚出去的内容在 padding 区继续显示。
修复：Word/VSCode/邮件的滚动容器 padding 归零，留白交给内层元素。

### 5. 伪装窗口透出主界面

根因：is-boss-normal 未隐藏 #app，阅读器章节标题从伪装后面透出。
修复：body.is-boss-normal #app { display: none; }
